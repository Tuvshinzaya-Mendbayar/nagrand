import styled from 'styled-components';
import {
	space,
	color,
} from 'styled-system';

const ImgWrapper = styled('img')(
	{
		padding: 0,
		margin: "1em",
	}
	,
	color,
	space,
)

export default ImgWrapper;